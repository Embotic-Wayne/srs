https://github.com/Embotic-Wayne/srs

# Log Archive Tool

A simple Bash script that archives logs by compressing a given directory into a timestamped `.tar.gz` file.

## Usage

```bash
chmod +x log-archive.sh
./log-archive.sh <log-directory>